import 'package:flutter/material.dart';

/// Clé globale pour la navigation depuis les notifications (hors arbre widget).
final GlobalKey<NavigatorState> appNavigatorKey = GlobalKey<NavigatorState>();
